import express from"express";import cors from"cors";import cookieParser from"cookie-parser";const app=express();app.use(cors({origin:process.env.CORS_ORIGIN,credentials:!0})),app.use(cookieParser()),app.use(express.json({limit:"24kb"})),app.use(express.urlencoded({extended:!0,limit:"24kb"})),app.use(express.static("public"));export default app;